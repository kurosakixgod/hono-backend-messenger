services:
  # PostgreSQL Database
  postgres:
    image: postgres:17.4
    container_name: messenger_postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: messenger_db
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/db/init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - messenger_network

  # # Hono Backend
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: messenger_backend
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     NODE_ENV: development
  #     DATABASE_URL: postgresql://postgres:postgres@postgres:5432/messenger_db
  #     PORT: 3000
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ./src:/app/src
  #     - ./package.json:/app/package.json
  #     - /app/node_modules
  #   networks:
  #     - messenger_network

volumes:
  postgres_data:
    driver: local

networks:
  messenger_network:
    driver: bridge

