{
  "name": "hono",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "migrate:refresh-tokens": "bun run src/migrate-refresh-tokens.ts",
    "test:refresh-tokens": "bun run src/test-refresh-tokens.ts",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.2.0",
    "@types/bun": "latest",
    "eslint": "^9.39.1"
  }
}
